import { Link } from 'react-router-dom';

export const Requests = () => {
    return (
        <div>
            <Link to={'new'}>{'new'}</Link>
            <Link to={'2234234'}>{'edit'}</Link>
        </div>
    );
};
